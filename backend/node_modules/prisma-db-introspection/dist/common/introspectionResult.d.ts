import { Renderer, DatabaseType, ISDL } from 'prisma-datamodel';
export declare abstract class IntrospectionResult {
    renderer: Renderer;
    databaseType: DatabaseType;
    constructor(databaseType: DatabaseType, renderer?: Renderer);
    abstract getDatamodel(): Promise<ISDL>;
    renderToDatamodelString(): Promise<string>;
    getNormalizedDatamodel(baseModel?: ISDL | null): Promise<ISDL>;
    renderToNormalizedDatamodelString(baseModel?: ISDL | null): Promise<string>;
}
